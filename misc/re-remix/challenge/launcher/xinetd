service challenge
{
    disable     = no
    type        = UNLISTED
    socket_type = stream
    protocol    = tcp
    port        = 1337
    wait        = no
    user        = nobody
    server      = /usr/local/bin/python
    server_args = -u /app/app.py
}
